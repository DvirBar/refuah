import { expect } from "chai";

describe("test getByIdOrFail", () => {
    it("should return an existing document that has the id provided", () => {
        expect(true).to.be.true;
    });

    it("should throw an error message if id doesn't exist", () => {
        expect(true).to.be.true;
    });
});
